vaadin:
  launch-browser: true

spring:
  application:
    name: spring-react-vaadin

  ai:
    ollama:
      base-url: http://localhost:11434
      chat:
        model: mistral
        options:
          temperature: 0.7
          top-p: 0.9

  datasource:
    url: jdbc:h2:mem:products-db

  h2:
    console:
      enabled: true

